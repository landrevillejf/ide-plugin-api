<configuration>
    <!-- Enable all log levels with a null appender so isXxxEnabled() guards
         return true (enabling branch coverage) without generating any output. -->
    <appender name="NULL" class="ch.qos.logback.core.helpers.NOPAppender"/>
    <root level="ALL">
        <appender-ref ref="NULL"/>
    </root>
</configuration>
